<div class="col-span-1 rounded-xl overflow-hidden transition">
    <figure>
    {{#if profile_image}}
        <img src="{{profile_image}}" alt="{{name}}" class="w-full h-64 rounded-2xl object-cover">
    {{/if}}
    {{^if profile_image}}
        <img src="{{asset "images/default_avatar_612.jpg"}}" alt="default" class="w-full h-64 rounded-2xl object-cover">
    {{/if}}
    </figure>
    <div class="py-4">
        <h3 class="font-bold text-lg">{{name}}</h3>
        <p class="text-sm text-gray-600 mt-2">{{bio}}</p>
    </div>
</div>
